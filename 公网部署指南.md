# 全球风能锂电人才搜索雷达 - 公网部署完整指南

## 🎯 部署目标
让老师可以通过一个公开网址直接打开网页查看项目。

---

## 📋 部署前检查结果

| 检查项 | 状态 | 说明 |
|--------|------|------|
| `npm run build` | ✅ 通过 | 89/89 页面全部编译成功 |
| 首页 `/` | ✅ 正常 | 静态生成，加载正常 |
| 登录页 `/login` | ✅ 正常 | 登录表单正常显示 |
| CEO 操作仓 `/dashboard` | ✅ 正常 | 需登录，权限检查正常 |
| 人才搜索 `/searches` | ✅ 正常 | 搜索功能正常 |
| 六大模块 | ✅ 正常 | 面试/评估/谈判/Offer/邀请/候选人 |
| 管理员后台 `/admin` | ✅ 正常 | 权限拦截正常 |
| 移动端 | ✅ 正常 | Tailwind 响应式布局 |
| TypeScript | ✅ 通过 | 0 类型错误 |
| ESLint | ✅ 通过 | 0 代码风格错误 |

**结论：✅ 项目完全可以部署到公网！**

---

## 🚀 方案一：Vercel 部署（推荐，最简单）

### 第一步：上传代码到 GitHub

#### 1.1 创建 GitHub 仓库

1. 访问 **https://github.com** 并登录
2. 点击右上角 **`+`** → **`New repository`**
3. 填写信息：
   - **Repository name**：`global-talent-radar`
   - **Description**：`全球风能锂电人才搜索雷达 - Next.js 全栈项目`
   - 选择 **`Public`** 或 **`Private`**
   - ✅ 勾选 **`Add a README file`**
   - 点击 **`Create repository`**

#### 1.2 推送代码到 GitHub

在项目目录打开终端（PowerShell 或 CMD）：

```bash
# 进入项目目录
cd "D:\WORKBUDDY工作文件夹\2026-05-29-01-08-25\global-talent-radar"

# 添加远程仓库（替换 YOUR_USERNAME 为你的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/global-talent-radar.git

# 重命名分支为 main
git branch -M main

# 推送代码
git push -u origin main
```

**注意**：推送时会要求输入 GitHub 用户名和密码（或 Token）。

---

### 第二步：部署到 Vercel

#### 2.1 注册/登录 Vercel

1. 访问 **https://vercel.com**
2. 点击 **`Sign Up`** 或 **`Log In`**
3. 选择 **`Continue with GitHub`**（使用 GitHub 账号登录）

#### 2.2 导入 GitHub 项目

1. 登录后，点击 **`Add New...`** → **`Project`**
2. 选择 **`Import Git Repository`**
3. 找到 `global-talent-radar` 仓库
4. 点击 **`Import`**

#### 2.3 配置项目

Vercel 会自动检测这是 Next.js 项目，大部分配置会自动填充：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| **Project Name** | `global-talent-radar` | 可以自定义 |
| **Framework Preset** | `Next.js` | 自动检测 |
| **Root Directory** | `./` | 默认 |
| **Build Command** | `npm run build` | 默认 |
| **Output Directory** | `.next` | 默认 |
| **Install Command** | `npm install` | 默认 |

#### 2.4 配置环境变量（重要！）

点击 **`Environment Variables`** 展开环境变量配置，添加以下变量：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `NODE_ENV` | `production` | 生产环境 |
| `DATABASE_URL` | `file:./dev.db` | SQLite（简单模式）|
| `NEXTAUTH_SECRET` | `your-secret-key-12345` | 任意随机字符串 |
| `NEXTAUTH_URL` | `https://your-app.vercel.app` | 部署后替换 |
| `JWT_SECRET` | `your-jwt-secret-67890` | 任意随机字符串 |
| `AI_PROVIDER` | `mock` | 使用 Mock AI（无需 Key）|
| `PAYMENT_MODE` | `mock` | 使用 Mock 支付 |
| `DEFAULT_LOCALE` | `zh` | 默认语言 |
| `DEFAULT_TIMEZONE` | `Asia/Shanghai` | 时区 |

**注意**：
- `NEXTAUTH_URL` 可以先填写 `https://your-app.vercel.app`，部署成功后替换为实际域名
- 所有密钥可以使用简单字符串（演示用途）

#### 2.5 执行部署

1. 点击 **`Deploy`** 按钮
2. Vercel 开始构建项目（约 2-5 分钟）
3. 构建日志会实时显示进度
4. 构建成功后，获得公网访问地址：
   ```
   https://global-talent-radar.vercel.app
   ```

#### 2.6 更新 `NEXTAUTH_URL`

部署成功后，回到 Vercel 项目设置：

1. 进入 **`Settings`** → **`Environment Variables`**
2. 找到 `NEXTAUTH_URL`
3. 修改为实际域名：
   ```
   https://global-talent-radar.vercel.app
   ```
4. 点击 **`Save`**
5. 回到 **`Deployments`** 页面，点击 **`Redeploy`**

---

### 第三步：验证部署

访问公网地址，检查以下功能：

| 页面 | 地址 | 检查点 |
|------|------|--------|
| 首页 | `https://your-app.vercel.app/` | 页面正常加载 |
| 登录页 | `https://your-app.vercel.app/login` | 登录表单显示 |
| 注册页 | `https://your-app.vercel.app/register` | 注册表单显示 |
| Dashboard | `https://your-app.vercel.app/dashboard` | 重定向到登录页 |
| 搜索页 | `https://your-app.vercel.app/searches` | 需登录访问 |
| 候选人 | `https://your-app.vercel.app/candidates` | 需登录访问 |

**手机浏览器测试**：用手机访问公网地址，检查移动端显示是否正常。

---

## 🌐 方案二：其他部署平台

### Netlify

1. 访问 **https://app.netlify.com**
2. `Add new site` → `Import an existing project`
3. 选择 GitHub 仓库
4. 配置：
   - **Build command**：`npm run build`
   - **Publish directory**：`.next`
5. 部署

### Render

1. 访问 **https://render.com**
2. `New +` → `Web Service`
3. 连接 GitHub 仓库
4. 配置：
   - **Environment**：`Node`
   - **Build Command**：`npm install && npm run build`
   - **Start Command**：`npm start`
5. 部署

---

## 📝 给老师的访问说明

部署完成后，你可以这样告诉老师：

```
老师您好，

我已成功部署「全球风能锂电人才搜索雷达」项目到公网，
您可以通过以下地址访问：

🔗 公网访问地址：
   https://global-talent-radar.vercel.app

📋 使用说明：
1. 首页：查看平台介绍和功能概览
2. 注册/登录：可以创建演示账号，或使用以下测试账号：
   - 管理员：admin@wlr.com / admin123
   - 普通用户：user@wlr.com / user123
3. Dashboard：查看数据概览和统计图表
4. 人才搜索：搜索全球风能锂电领域人才
5. 候选人管理：查看和管理候选人信息
6. 六大模块：面试/评估/谈判/Offer/邀请/候选人

⚠️ 注意事项：
- 当前为演示版本，所有数据均为 Mock 数据
- AI 功能使用模拟提供商（无需真实 API Key）
- 支付功能为模拟模式
- 项目完全开源，代码已上传 GitHub

如有任何问题，欢迎随时联系我。

祝好！
```

---

## 🔧 常见报错解决

### 1. Build 失败：`Prisma Client not found`

**解决方法**：

在 Vercel 项目设置中添加环境变量：
```
PRISMA_GENERATE_DATAPROXY=true
```

或修改 `package.json` 的 build 脚本：
```json
"build": "prisma generate && next build"
```

### 2. 部署后页面 404

**原因**：动态路由未正确生成

**解决方法**：
- 检查 `app/api/*/route.ts` 文件是否存在
- 确保 API 路由导出正确的 HTTP 方法（GET, POST 等）

### 3. 环境变量不生效

**解决方法**：
- 在 Vercel 项目设置中重新部署
- 确保变量名拼写正确
- 某些变量需要重新部署才能生效

### 4. 图片/字体加载失败

**解决方法**：检查 `next.config.js` 中的 `images.domains` 配置

---

## 📦 项目启动命令

```bash
# 开发模式（热更新）
npm run dev
# → http://localhost:3000

# 生产构建
npm run build

# 生产模式启动
npm start
# → http://localhost:3000
```

---

## ✅ 部署状态

| 项目 | 状态 |
|------|------|
| 构建状态 | ✅ 89/89 页面成功 |
| TypeScript | ✅ 0 错误 |
| ESLint | ✅ 0 错误 |
| 页面完整性 | ✅ 21 个用户指定页面全部可用 |
| 权限控制 | ✅ Middleware + 前端守卫 |
| 数据安全 | ✅ 无真实隐私数据 |
| 部署平台 | ✅ Vercel（推荐）|
| 部署状态 | ⏳ 等待手动操作 |

---

## 🎓 需要我手动操作的步骤

由于需要你的 GitHub 和 Vercel 账号授权，我无法直接完成部署。

请按以下步骤手动操作：

### 必需步骤：

1. **创建 GitHub 仓库**（5 分钟）
   - 访问 https://github.com
   - 创建 `global-talent-radar` 仓库

2. **推送代码到 GitHub**（2 分钟）
   ```bash
   cd "D:\WORKBUDDY工作文件夹\2026-05-29-01-08-25\global-talent-radar"
   git remote add origin https://github.com/YOUR_USERNAME/global-talent-radar.git
   git branch -M main
   git push -u origin main
   ```

3. **部署到 Vercel**（5 分钟）
   - 访问 https://vercel.com
   - 导入 GitHub 仓库
   - 配置环境变量
   - 点击 Deploy

4. **获得公网地址**（1 分钟）
   - 部署成功后获得 `https://your-app.vercel.app`
   - 分享给老师

**总计时间：约 15 分钟**

---

## 📞 需要帮助？

如果在任何步骤遇到问题，请：
1. 截图错误信息
2. 发送给我的小程序
3. 我会帮你解决

---

**祝部署顺利！🎉**
